## 一、任务背景

本任务要求你参考以下两个开源仓库，使用 ROS2 和 AI 辅助编程/vibe coding，完成一个小型无人机仿真器的编程任务：

1. pengyu_sim: [https://gitee.com/potato77/pengyu_sim](https://gitee.com/potato77/pengyu_sim)
2. MARSIM: [https://github.com/hku-mars/MARSIM](https://github.com/hku-mars/MARSIM)  （这个有论文依托，但不是很重要）

注意：参考仓库主要用于理解无人机动力学、控制、可视化和仿真系统组织方式。你提交的成果必须是 ROS2 工程，不是简单运行、复制或包装原仓库。

## 二、任务目标

实现一个可运行的 ROS2 无人机仿真系统，至少包含：

1. 无人机动力学模块：输入为 4 个电机转速 RPM；
2. 无人机控制器模块：输入为目标点，输出为 4 个电机转速 RPM；
3. 地图模块：能够加载或生成障碍物地图；（选做）
4. 避障模块：无人机飞向目标点时需要考虑障碍物并生成安全路径或安全局部目标点；（选做）
5. 可视化：可使用 RViz2，也可自行实现简易地面站（qt或者web地面站）；
6. 完整 README。

最终提交内容为：

1. Git 仓库地址（开源，我可以直接看到大家的代码架构，并且方便其他人能迅速编译和使用你的仓库）；
2. PDF 报告（讲清楚你怎么做的，结果是啥，这个功能背后的原理是什么，有什么样的后续用途等等）；
3. 1 到 3 分钟演示视频（录屏）。
- 这个任务我主要想考核的是大家对无人机动力学、控制的理解，以及整体的编程能力（包括对git的使用）
- 如果觉得有难度，可以不做地图和避障模块

## 三、推荐技术环境

建议环境：

1. Ubuntu 22.04；
2. ROS2 Humble；
3. C++ 或 Python 均可，推荐核心动力学和控制器使用 C++；
4. 可使用 Eigen、tf2、rviz2、rqt_plot、PlotJuggler 等工具；

如果使用其他 ROS2 版本，请在 README 中明确说明。

## 四、系统功能要求

### 1. ROS2 工程结构

仓库应至少包含以下模块或 package：（仅作参考）

```text
your_drone_sim/
  README.md
  src/
    drone_dynamics/
    drone_controller/
    drone_map/
    drone_planner/
    drone_visualization/   # 可选，也可以使用 RViz2 配置文件
    drone_msgs/            # 可选，若使用自定义消息
  launch/
  config/
  report/
```

不强制完全按上述结构，但必须模块清楚、能一键启动。

### 2. 动力学节点

实现 `quadrotor_dynamics_node` 或同等功能节点。

输入：

1. 4 个电机转速 RPM，例如 `/drone/motor_rpm_cmd`；
2. 建议使用 `std_msgs/msg/Float32MultiArray` 或自定义 `MotorRPM.msg`。

输出：

1. `/drone/odom`，类型建议为 `nav_msgs/msg/Odometry`；
2. `/drone/imu`，类型建议为 `sensor_msgs/msg/Imu`，可简化；
3. `/tf`，发布 `map -> base_link`；
4. `/drone/path`，类型建议为 `nav_msgs/msg/Path`。

动力学至少包含：

1. 状态量：位置、速度、姿态四元数、机体系角速度；
2. 电机一阶响应：实际转速逐渐逼近期望转速；
3. 推力模型：`F_i = k_F * omega_i^2`；
4. 力矩模型：根据 X 型四旋翼布局由 4 个电机推力和反扭矩产生 roll/pitch/yaw torque；
5. 平动方程：由姿态旋转后的总推力和重力更新速度与位置；
6. 转动方程：`I * omega_dot = tau - omega x (I * omega)`；
7. 四元数姿态积分与归一化；
8. 电机转速上下限保护。

### 3. 控制器节点

实现 `position_controller_node` 或同等功能节点。

输入：

1. 目标点 `/drone/goal`，类型可为 `geometry_msgs/msg/PoseStamped`；
2. 当前状态 `/drone/odom`。

输出：

1. `/drone/motor_rpm_cmd`，即 4 个电机 RPM。

控制器至少实现：

1. 位置误差到期望加速度的 PID 或 PD 控制；
2. 由期望加速度计算期望 roll、pitch、yaw；
3. 姿态或角速度控制，输出总推力和三轴力矩；
4. mixer：将总推力和三轴力矩分配为 4 个电机转速；
5. 推力、姿态角、RPM 的限幅；
6. 基本防炸机制，例如目标点过远时限速/限加速度。

### 4. 地图模块

实现 `map_server_node`、`obstacle_map_node` 或同等功能节点。

地图形式可任选一种：

1. 简单几何障碍物地图：若干球体、圆柱体、立方体或墙；
2. 栅格/体素地图：2D occupancy grid 或 3D voxel grid；
3. 点云地图：可参考 MARSIM 的点云地图思路，发布 `sensor_msgs/msg/PointCloud2`；
4. 程序随机生成地图，但必须支持固定随机种子，保证可复现实验。

地图模块至少输出：

1. `/map/obstacles`，可使用 MarkerArray、PointCloud2、OccupancyGrid 或自定义消息；
2. RViz2 中可见的障碍物；
3. README 中说明地图坐标系、障碍物尺寸和安全距离。

最低地图要求：至少包含 5 个静态障碍物，且障碍物必须位于起点到目标点之间，能迫使无人机改变路径。

### 5. 避障/规划模块

实现 `local_planner_node`、`safe_goal_node` 或同等功能节点。

输入：

1. 当前状态 `/drone/odom`；
2. 目标点 `/drone/goal`；
3. 地图或障碍物 `/map/obstacles`。

输出：

1. 给控制器的安全局部目标点、waypoint 或速度/加速度参考；
2. 建议 topic 为 `/drone/reference`、`/drone/waypoints` 或 `/drone/safe_goal`。

避障方法可以较简单，但必须可解释。可选方法包括：

1. A*/Dijkstra/RRT/PRM 等全局路径搜索；
2. 人工势场、采样轨迹、pure pursuit + collision checking；
3. 对直线路径做碰撞检测，若碰撞则生成绕行 waypoint；
4. 对静态障碍物保持安全距离的局部目标点生成器。

最低避障要求：

1. 无人机从起点飞到目标点时不得穿过障碍物；
2. 需要设置安全距离，例如 0.3 m 或 0.5 m；
3. 可视化中能看到规划路径或局部目标点；
4. 报告中必须说明避障算法的输入、输出、失败条件。

### 6. 可视化

至少完成以下一种：

方案 A：RViz2 可视化

1. 显示无人机模型或 Marker；
2. 显示目标点；
3. 显示历史轨迹；
4. 显示地图障碍物；
5. 显示规划路径、waypoints 或局部安全目标点；
6. 可以通过 RViz2 的 2D/3D Goal 或命令行发布目标点。

方案 B：简易地面站

1. 显示当前位置、目标点、速度和姿态；
2. 支持输入目标点；
3. 显示障碍物地图；
4. 显示轨迹曲线或二维/三维运动图；
5. 显示是否触发避障、最小障碍物距离等状态。

## 五、最低验收场景

请在 README 和报告中展示以下实验：

1. 悬停实验：无人机从地面或初始高度起飞并稳定在 `(0, 0, 1.5)` 附近；
2. 目标点实验：输入目标点 `(2, 1, 1.5)`，无人机能飞向目标并悬停；
3. 多目标点实验：按顺序飞到 3 到 4 个目标点，例如正方形航线；
4. 静态避障实验：地图中至少 5 个障碍物，无人机从起点飞到目标点并绕开障碍物；
5. 狭窄通道或绕行实验：设置一个需要明显绕行的场景，展示规划路径和实际轨迹；
6. 稳定性展示：给出位置误差曲线、RPM 曲线、轨迹图和最小障碍物距离曲线。

建议指标：

1. 最终位置误差；
2. 到达时间；
3. 最大超调量；
4. 稳态误差；
5. 最小障碍物距离；
6. 路径长度或飞行时间；
7. 是否出现姿态发散或 RPM 饱和。

最低要求：在合理参数下，目标点悬停误差应能收敛到 0.3 m 以内。

避障实验中，实际轨迹与障碍物的最小距离应大于你设定的安全距离。

## 六、加分项

以下内容不是必须，但可用于拉开差距：

1. 支持参数文件配置质量、惯量、电机系数、控制增益；
2. 支持风扰或外力扰动，并展示恢复能力；
3. 支持传感器噪声、IMU 简化模型；
4. 支持点云地图、体素地图或局部感知范围模拟；
5. 支持多无人机；
6. 支持轨迹输入，如圆轨迹、八字轨迹或 waypoint list；
7. 提供单元测试或脚本化自动评测；
8. 写一个简洁可用的 Web/Qt/Python 地面站；
9. 对比自己的实现与 pengyu_sim/MARSIM 的设计差异。
10. 其他也行，以上都是AI生成的，请发挥自己的想象力和能力！

## 七、AI 辅助编程 / Vibe Coding 要求

允许并鼓励使用 AI 辅助编程，但必须体现你对代码的理解和验证能力。

请在仓库中提交 `ai_usage.md`，包含：

1. 使用了哪些 AI 工具；
2. 至少 8 条关键 prompt 或交互摘要；
3. AI 帮你完成了哪些模块；
4. 哪些核心公式和接口是你自己确认或修改的；
5. AI 生成过哪些错误，你如何发现并修正；
6. 你如何验证动力学、控制器和 ROS2 topic 是否正确。

不如实说明 AI 使用情况会影响评分。

## 八、报告要求

报告建议 6 到 10 页，包含：

1. 系统架构图：节点、topic、消息类型、数据流；
2. 动力学模型：状态量、输入、推力/力矩/积分公式；
3. 控制器设计：位置环、姿态环、mixer、限幅策略；
4. 地图与避障设计：地图表示、碰撞检测、安全距离、规划/避障算法；
5. 可视化方案；
6. 实验结果：轨迹图、误差曲线、最小障碍物距离、参数表、失败案例；
7. 与参考仓库的关系：参考了什么，重写了什么，为什么这样设计；
8. AI 使用说明；
9. 反思：如果继续做两周，最应该补什么。