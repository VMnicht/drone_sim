# AI 使用记录

本文件随开发过程持续更新，最终版本将满足任务文档要求的不少于 8 条关键交互记录。

## 工具

- OpenAI Codex：任务分析、架构设计、代码实现辅助和测试诊断。

## 当前关键交互

### 1. 阅读任务并制定工程计划

- 目标：从空仓库规划 ROS2 无人机仿真器。
- AI 参与：整理模块边界、topic、动力学/控制/规划方案和验收矩阵。
- 人工确认：地图和避障虽然标为选做，但最低验收包含相关场景，因此纳入主线。

### 2. 强化算法与 ROS2 解耦

- 目标：控制算法、动力学模型等不能依赖 ROS2，地图相关功能可以耦合 ROS2。
- AI 参与：提出 `drone_core` 与 ROS2 adapter 的单向依赖设计。
- 人工确认：核心只使用 Eigen/STL，显式传入状态、命令和 `dt`；节点只做通信和转换。

### 3. 建立框架和动力学模块

- 目标：建立 ROS2 packages，并实现首版六自由度动力学。
- AI 参与：生成 package 骨架、电机模型、刚体方程、节点、YAML、launch 和测试。
- 人工验证：执行 `colcon build`、8 个核心测试、ROS2 topic/TF/service 检查，以及持续悬停 RPM 命令测试。
- 已发现的问题：使用单次 `ros2 topic pub` 时，CLI 建连时间超过 0.5 秒安全超时，读取到的实际 RPM 已下降到零。改用 50 Hz 持续命令后确认实际转速稳定到 10820.66 RPM，证明接口和超时保护均按设计工作。

### 4. 模型控制、自动起飞和 RViz2

- 目标：完成模型控制器，让带电机一阶响应的动力学从地面起飞到 1.5 m，并在 RViz2 显示飞机。
- AI 参与：实现基于质量/惯量的几何控制、统一 mixer、零值扰动入口、ROS2 控制节点、飞机 Marker 和 RViz 配置。
- 人工验证：纯核心闭环测试与真实 ROS2 运行都达到 1.5 m；检查最终速度、RPM、Marker 数量和 RViz OpenGL 初始化日志。
- 已发现的问题：最初用绝对行列式阈值判断分配矩阵可逆性，但矩阵元素约为 `1e-6`，导致可逆矩阵被误判为奇异。改用 Eigen `FullPivLU::isInvertible()` 后，mixer 往返测试和闭环测试通过。
- 已发现的问题：可视化 Python 脚本在 symlink-install 下缺少可执行位，launch 无法找到 libexec。补齐可执行权限并重跑后，飞机 8 个 Marker 和目标 Marker 均正常发布。

### 5. 修复 RViz2 飞机闪烁

- 目标：解决 Drone Model 状态在 OK/Error 间切换以及 Marker 闪烁。
- AI 参与：分析 Marker 时间戳、重复静态发布与 `/tf` 到达顺序之间的竞争关系。
- 人工验证：第一轮零时间戳和 `frame_locked=true` 缓解了问题；第二轮改为 Reliable + Transient Local 一次性发布，解决闪烁但暴露了 RViz 在首次 TF 尚未建立时可能永久丢失模型的问题；最终采用 1 Hz 低频刷新，让 RViz 最多 1 秒自动恢复模型，同时避免原来 20 Hz 反复进入 TF 消息过滤器。

### 6. 自动实验与指标工具

- 目标：脚本化生成任务文档要求的位置、误差、RPM、姿态和轨迹结果。
- AI 参与：实现 ROS2 记录节点、CSV/JSON 汇总和 Matplotlib 图表。
- 人工验证：分别运行悬停、目标点和多航点实验，并用独立脚本检查 0.3 m 误差、倾角和 RPM 饱和门槛。
- 已发现的问题：Ubuntu 的 Matplotlib 3.5 不支持新版 `seaborn-v0_8-whitegrid` 名称，改用稳定的内置 rc 参数；同时将绘图库延迟加载，防止漏记起飞初段。

### 7. 水平闭环振荡诊断

- 目标：完成 `(2,1,1.5)` 目标点实验。
- AI 初始结果：第一组水平实验出现约 0.7 秒周期振荡，最终误差 0.48 m，不满足验收。
- 人工修正：提高姿态角速度阻尼，将水平加速度限制从 5 降为 3 m/s²、倾角限制从 35° 降为 25°，并加入带电机滞后的三维目标闭环单元测试。
- 验证结果：最终误差约 0.0027 m，任务完成且无 RPM 饱和。

### 8. 多航点与 yaw 耦合失败案例

- 目标：完成方形多目标点任务。
- AI 初始结果：位置切换时同步施加 90°/180° yaw 阶跃，实际轨迹出现明显大圆弧，路径长度约 14.6 m。
- 人工判断：任务只要求位置航点，不要求航向阶跃；当前控制器也没有平滑 yaw 轨迹的角速度/角加速度前馈。
- 修正与验证：航线改为固定 yaw，5/5 航点完成，最终误差约 0.020 m，限制作为已知问题如实记录。

### 9. 早期三场景报告与演示材料

- 目标：先为 hover/target/square 生成可复现实验图、PDF 报告和短演示视频，验证交付生成链。
- AI 参与：从真实 ROS2 CSV 遥测生成实验图、数据驱动 MP4 和报告排版。
- 人工验证：检查视频帧数、分辨率和解码状态；PDF 逐页渲染后检查文字、表格和图片是否裁切或重叠。

### 10. 干净构建与 ROS2 集成测试

- 目标：证明当前提交可从空缓存构建，并把 topic、TF、Marker 与闭环起飞检查从人工步骤变成自动测试。
- AI 参与：增加无 ROS standalone CMake 模式、GitHub Actions 和 `launch_testing` 悬停测试。
- 人工验证：首次 standalone 配置发现 `ament_export_*` 仍残留在独立分支外，修正后无 ROS 构建通过；首次 launch 测试使用了非 Humble API，功能断言通过但退出码断言报错，改为 `launch_testing.asserts.assertExitCodes` 后全量通过。
- 最终覆盖：Odometry、RPM 命令、`map -> base_link` TF、Transient Local Marker、1.5 m 起飞误差、RPM 有限性和节点正常退出。

### 11. 全量 YAML 参数化与免 source 启动

- 目标：消除节点中的不可调运行常量，让模型、控制、接口、可视化、实验和启动参数都可以直接通过 YAML 调节，同时保留算法与 ROS2 的解耦边界。
- AI 参与：新增共享 `model.yaml`、`interfaces.yaml` 及分层配置文件，把 C++/Python 节点的频率、QoS、阈值、话题、Marker 几何和场景参数改为声明参数；launch 按共享配置到场景配置的顺序加载。
- 人工验证：参数覆盖脚本逐项对照节点声明与每一种实际 launch 加载链，结果为动力学 35/35、控制器 36/36、可视化 31/31、航点任务 13/13、记录器 17/17，共 132 项声明参数；同时反向检查 YAML 拼写，完整构建和 21 项测试均通过。
- 运行验证：在独立 ROS domain 中确认动力学、控制器和可视化节点读到同一组质量、机臂与接口参数，并实际运行目标点场景验证场景覆盖、记录器判据和命令行覆盖。
- 启动改进：`start_hover.sh` 在脚本内部自动加载 ROS2 和工作区，未构建时自动执行 `colcon build --symlink-install`，用户无需在当前终端手动 `source`。

### 12. 解析轨迹、风扰和传感器模型扩展

- 目标：完成圆/八字轨迹、外力扰动和带噪传感器三项加分功能，同时保持数学模型与 ROS2 解耦。
- AI 参与：在 `drone_core` 中实现显式时间输入的轨迹生成器、扰动模型和传感器噪声模型；ROS2 节点只完成 YAML 配置与消息转换；补充 GPS、真值/带噪 topic 及协方差。
- 人工验证：固定随机种子重复 reset，检查输出完全一致；核对 `sensor_noise` 样本标准差与 YAML；运行 circle、figure-eight、wind_gust 三类闭环场景。
- 发现并修复：噪声模型 reset 后分布对象仍保留内部缓存，首次重复序列不一致；reset 时同步重置分布对象后确定性测试通过。

### 13. 地图、局部点云和三维规划

- 目标：实现静态/随机障碍物、简化局部感知、体素占用和 3D A* 避障。
- AI 参与：实现 box/cylinder 表面采样、FOV/量程/遮挡/noise/dropout 点云、体素 Marker、6/18/26 邻接 A*、膨胀、路径简化与 lookahead 跟踪。
- 人工验证：运行 five_obstacles、narrow_passage、perception_replan，检查 `goal_reached`、点数与几何净间隙，并在 RViz2 同时显示地图、点云、体素和规划路径。
- 发现并修复：初始安全参数下窄通道的规划膨胀与目标位置不匹配；统一机体半径/安全裕度、减小局部点膨胀并把目标移到通道出口后，净间隙提高到 0.214 m 且任务完成。

### 14. 三机、Web 地面站与故障注入

- 目标：完成多无人机、地面站和创新故障扩展。
- AI 参与：建立三套 namespace/TF/传感器实例与 fleet monitor；实现本地 HTTP 状态/曲线/目标/reset/故障接口；在控制命令链加入电机效率/上限和 dropout/delay/freeze 故障节点。
- 人工验证：专项脚本确认三机均活跃、最小间距 0.788 m、0 次违规；Web GET/POST 返回 200/202；故障场景确认 100 个命令被修改后仍安全恢复。
- 设计限制：多机当前提供独立任务与安全监测，不宣称已实现分布式在线互避；Web 默认仅绑定本机地址。

### 15. 全场景自动评测、回放和参数扫描

- 目标：把所有加分功能从“能运行”提升为可重复、可阈值验收。
- AI 参与：扩展记录器的扰动、噪声、规划、净间隙和点云指标；实现 11 场景批处理、按场景阈值验证、确定性回放比较及 3×3 控制增益/风力扫描热图。
- 人工验证：11/11 场景全部通过；wind 两次回放各指标差不超过 0.0084（容差 0.08）；扫描 9 组均生成独立配置、摘要、CSV 与热图。
- 发现并修复：记录器同时导入 `pathlib.Path` 和 `nav_msgs.msg.Path` 导致类型名覆盖；将 ROS 消息显式别名为 `NavPath` 后恢复文件路径处理。

### 16. 参考项目审阅与诚实对比

- 目标：完成与 pengyu_sim、MARSIM 的可追溯比较，不夸大本工程的感知真实性。
- AI 参与：阅读公开仓库与 MARSIM 论文，比较 ROS 版本、动力学边界、PCD/LiDAR 渲染、规划、多机和评测范围，整理到 `docs/reference_comparison.md`。
- 人工校验：记录来源 URL、pengyu_sim 检查提交和检查日期；将不同硬件/地图下的数值明确标记为不可直接排名。
- 结论：本工程偏重轻依赖 ROS2 控制/规划教学与自动验收；MARSIM 更适合真实点云 LiDAR；pengyu_sim 更适合 ROS1 Sunray/MAVROS/PX4 语义联调。

### 17. 最终回归和交付文档同步

- 目标：确保代码、参数、结果、README、规划和审计不会处于不同开发阶段。
- AI 参与：重写 README/参数指南/完成性审计，加入全部 10 项加分证据；更新报告和演示生成链，使其读取最新 YAML 与 11 场景 artifacts。
- 人工验证：13 类节点参数覆盖检查通过；全量 `colcon build --symlink-install` 通过；WSLg 中 RViz2 完成 OpenGL 4.2 初始化且无显示插件错误。最终汇总数以后续第 19、20 节为准。
- 外部待办：公开 Git 远端创建和推送需要仓库所有者账号授权，不由 AI 擅自执行。

### 18. 安全合同与可追溯证据复核

- 目标：复核“规划安全距离”是否由配置、规划器、记录器和验收器共同执行，而不是只在文档中声明。
- AI 初始发现：旧验收只要求几何净距大于零，三组实测净距 0.162/0.214/0.193 m 并未满足任务建议的 0.3 m 安全距离。
- 人工确认与修正：统一机体半径、0.50 m 规划裕度和 0.30 m 实测净距合同，降低路径前视距离后重跑三组场景；当前正式批次净距为 0.440/0.399/0.481 m，均完成任务。验证器增加跨 YAML 安全参数一致性检查。
- 证据改进：批量实验保存配置哈希、完整参数快照、Git 状态、运行日志和按场景 rosbag；独立证据脚本检查包索引、消息数及必需 topic。

### 19. 规划承诺缺口与地面站补齐

- 目标：对照更新后的工程规划，补齐扰动运行开关、传感器输出延迟、体素过期衰减和 Web 结果查看。
- AI 参与：动力学新增 YAML 初始状态与 SetBool 扰动开关；传感器适配层新增保留原测量时间戳的 Odom/IMU、GPS 独立延迟队列；局部体素新增持久时间、命中阈值和容量限制；Web 新增扰动控制与结果 API/表格。
- 人工验证：定向构建通过，Web 状态/结果 GET 为 200，目标、扰动、故障和 reset POST 为 202；最终 13 类节点共 339 项参数全部由真实 launch 配置覆盖。

### 20. 免 source 模式 Panel 与调参安全闭环

- 目标：把悬停、11 个正式场景、三机、地面站和批量评测集中到一个可直接启动的界面，同时让全部仿真/控制参数可安全调节。
- AI 参与：实现本机模式管理 Panel、各模式独立脚本、进程互斥和分级停止、实时日志、RViz 开关、结果来源区分及 29 份 YAML 编辑；保存采用自动备份、原子替换、全参数覆盖与跨配置安全合同校验，失败自动回滚。
- 人工验证：浏览器逐项检查 5 种模式与 11 个场景；专项测试验证路径穿越拒绝、非法 YAML 返回 400、并发模式返回 409、停止清理和 3 秒独立实验落盘；该测试已纳入 `colcon test`。
- 发现并修复：早期 HTTP 层未捕获 `yaml.YAMLError`，非法 YAML 会断开连接；补充统一错误映射后稳定返回 400。场景说明中的 YAML 行内逗号解析异常也已改为显式字符串。

### 21. WSL 运行稳定性、规划连续性与 RViz 性能优化

- 目标：解决运行不够丝滑、偶发失控、模型闪烁和长时间运行负载持续增长，同时保持原验收阈值不变。
- AI 诊断：实测发现重复仿真栈会把 Odom/TF 叠加到约 560 Hz；动力学每个 200 Hz 积分步发布全部状态并反复发送不断增长的 `Path`；规划器会在空间相近的旧路径段之间回跳；WSL 系统时钟回拨又使传感器用 ROS 时间做限流时连续丢弃约 1.3 秒 Odom。
- 修复：加入工作区级 `flock` 单实例锁；物理积分与状态/路径发布解耦并给路径设置 1200 点上限；规划器增加单调进度和到达终点锁定；传感器、轨迹、航点、故障、实验记录、体素过期、地面站 stale 判断和安全看门狗统一使用单调时钟，ROS 时间仅保留为消息时间戳；超时保护改为可配置等推力悬停。
- 诚实回滚：曾尝试按墙钟补做动力学积分以追赶 200 Hz，但完整场景出现圆/八字/噪声/避障真实退化，因此撤销该方案，恢复每次定时回调只推进一个固定 `dt`，没有放宽任何阈值。
- 验证：最终同批 11/11 场景及 rosbag 证据全部通过，11 份日志中超时/保护事件为 0；15 包全量构建、35 tests 全通过。RViz 实测 Odom/TF 95.23 Hz、最大间隔 12 ms，路径 4.762 Hz、模型 Marker 1.000 Hz，路径带宽早期约 44 KB/s、晚期约 115 KB/s且有界，Odom/TF 发布者各 1 个。
