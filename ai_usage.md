# AI 辅助编程使用说明

## 一、使用工具与基本原则

本项目使用 OpenAI Codex 作为辅助编程工具，主要用于拆分任务、提供代码骨架、整理重复性配置、分析报错和协助完善文档。AI 的输出只作为候选方案，不直接作为最终结论。

动力学方程、控制器结构、电机混控关系、算法与 ROS2 的边界、安全距离合同、参数选择以及最终验收均由本人主导并确认。对于 AI 生成或建议的内容，本人会先检查数学含义和接口约束，再通过单元测试、ROS2 集成测试和场景实验决定是否采用。

| 工作类型 | AI 的作用 | 本人的工作 |
| --- | --- | --- |
| 任务分析 | 整理需求、生成检查清单 | 确定实现范围、优先级和验收标准 |
| 核心算法 | 提供实现思路或代码骨架 | 推导并确认公式，修改接口、符号、限制器和参数 |
| ROS2 工程 | 辅助生成节点、launch、YAML 和测试模板 | 确定 topic、QoS、TF、命名空间和算法解耦边界 |
| 调试测试 | 提供可能原因和排查步骤 | 复现实验、读取日志、定位根因、选择修复方案并回归测试 |
| 文档整理 | 协助组织结构、图表和文字 | 核对事实、实验数据、限制条件和最终表述 |

## 二、本人负责的核心工作

### 1. 六自由度动力学与单位体系

本人确定了世界坐标系、机体系、四元数方向和状态量定义，逐项确认重力、推力、力矩、陀螺运动以及电机一阶响应的计算关系。推力采用 \(F_i=k_F\omega_i^2\)，反扭矩采用 \(\tau_i=k_M\omega_i^2\)，所有参数统一使用 SI 单位。本人还检查了四元数归一化、静止状态和悬停平衡，确认理论悬停转速约为 1133.15 rad/s（10821 rpm）。

### 2. 控制器、电机布局与混控

本人主导选择位置—速度—姿态串级控制结构，确认期望合力、期望姿态、姿态误差和角速度反馈的计算方式，并根据 X 型四旋翼的电机位置与旋转方向确定混控矩阵符号。水平加速度、最大倾角、总推力和电机转速限制均由本人结合闭环实验调整。AI 给出的控制骨架只有在混控往返测试、起飞测试和三维目标点测试通过后才被保留。

### 3. 算法与 ROS2 的接口边界

本人确定 `drone_core` 只依赖 Eigen/STL，不在控制算法、动力学模型、轨迹、扰动和传感器数学模型中使用 ROS2 类型。核心接口显式接收状态、控制量和 `dt`，ROS2 节点只负责参数读取、消息转换、时间调度与发布订阅。地图、点云和规划节点允许与 ROS2 耦合，这是根据任务性质作出的明确边界选择。

### 4. 地图规划与安全合同

本人确定规划器必须同时考虑机体半径、安全裕度和实际几何净距，并将 0.30 m 作为正式场景的最小净空验收条件。对目标点、膨胀半径、路径前视距离、路径进度单调性和失败条件进行多轮调整后，才形成最终参数。规划成功不能只看“找到路径”，还必须由记录器检查到达目标和真实净距。

### 5. 参数调试和最终验收

控制增益、倾角限制、轨迹速度、扰动强度、传感器噪声、安全距离和场景阈值均由本人结合实验结果决定。本人负责执行干净构建、单元测试、ROS2 集成测试、11 场景批处理、确定性回放和 RViz 可视化检查，并根据结果决定保留、修改或撤销 AI 建议。

## 三、AI 主要协助的内容

AI 的主要贡献集中在工程辅助工作：根据任务书整理模块清单；提供 package、节点、launch、YAML、测试和数据记录脚本的初始骨架；为报错给出排查方向；协助生成实验图表、README 和报告草稿；对重复性的参数声明、接口适配和文档格式进行整理。这些内容提高了开发效率，但核心设计和最终结果仍以本人的推导、选择和实测为准。

## 四、关键交互记录

以下为开发过程中有代表性的 12 次交互摘要。记录重点不是逐行复述对话，而是说明 AI 建议、本人决策和验证之间的关系。

| 序号 | 提示或问题摘要 | AI 提供的辅助 | 本人的确认、修改与验证 |
| --- | --- | --- | --- |
| 1 | “阅读任务文档，列出必须完成的模块和验收场景。” | 将需求拆成动力学、控制、通信、可视化、规划和实验模块。 | 本人对照任务书调整优先级，并决定把地图避障纳入主线、实现全部加分项。 |
| 2 | “控制算法和控制模型如何与 ROS2 解耦？” | 建议建立纯 C++ 核心库和 ROS2 adapter。 | 本人确定单向依赖规则、核心输入输出结构和 `dt` 语义，地图相关模块保留 ROS2 耦合。 |
| 3 | “给出 X 型四旋翼六自由度模型的代码骨架和测试清单。” | 提供状态更新、电机响应和刚体积分的初稿。 | 本人核对坐标系、单位、重力方向、四元数和推力/反扭矩符号，增加悬停平衡与能量合理性测试。 |
| 4 | “串级控制器怎样把目标点转换为四路电机转速？” | 提供几何控制和 mixer 的实现思路。 | 本人确定控制结构、电机编号和旋向，修正可逆性判断，调节阻尼、加速度与倾角限制，并完成 mixer 往返验证。 |
| 5 | “RViz 中 drone_model 为什么在 OK 和 Error 间切换？” | 提示检查时间戳、TF、Marker 生命周期、QoS 和重复发布者。 | 本人逐项测量 topic 频率和发布者数量，最终采用稳定 ID、Transient Local 和 1 Hz 低频刷新，确认模型不再闪烁。 |
| 6 | “如何自动记录位置误差、姿态、RPM 和场景结果？” | 提供记录器、CSV/JSON 汇总和绘图脚本骨架。 | 本人定义通过阈值、采样窗口和失效条件，用原始遥测复核汇总值，避免只依据图像判断成功。 |
| 7 | “目标点闭环出现约 0.7 秒振荡，应检查什么？” | 建议检查姿态阻尼、饱和限制和电机滞后。 | 本人复现实验并逐项调参，将水平加速度限制由 5 降为 3 m/s²、倾角限制由 35° 降为 25°，最终误差降至约 0.0027 m。 |
| 8 | “多航点任务加入 90°/180° yaw 阶跃后轨迹绕大圈。” | 建议增加平滑 yaw 轨迹或暂时固定航向。 | 本人根据任务只要求位置航点，选择固定 yaw，并把缺少 yaw 角速度/角加速度前馈列为已知限制。 |
| 9 | “圆、八字、风扰和传感器噪声怎样做可重复测试？” | 提供显式时间轨迹、随机种子和回放比较方案。 | 本人确定幅值、频率、风力和噪声参数，发现 reset 后随机分布仍有缓存并修正，随后验证重复运行的一致性。 |
| 10 | “三维体素 A* 怎样处理障碍膨胀、路径简化和安全距离？” | 提供邻接方式、体素膨胀与 lookahead 跟踪思路。 | 本人统一机体半径、安全裕度和 0.30 m 净空合同，调整目标与前视距离，按真实障碍几何重新验收。 |
| 11 | “WSL 长时间运行后频率异常、路径回跳，怎样定位？” | 提示检查重复进程、时钟回拨、路径增长和积分调度。 | 本人通过频率、带宽和日志实测定位问题；尝试追赶积分后发现回归退化，主动撤销该方案，保留固定 `dt` 并增加单实例锁和路径上限。 |
| 12 | “怎样比较 pengyu_sim 和 MARSIM，同时避免夸大结果？” | 协助整理公开仓库和论文的比较维度。 | 本人核对来源、版本和适用场景，明确不同地图与硬件下的数据不可直接排名，并如实记录本项目的简化感知和多机限制。 |

## 五、AI 建议中出现的问题与人工修正

AI 的建议并非总是正确。以下问题均由本人在编译、测试或实验中发现，并根据实际证据完成修正。

| 问题 | 发现方式 | 人工修正 |
| --- | --- | --- |
| 使用绝对行列式阈值判断 mixer 是否可逆，量级较小的正常矩阵被误判为奇异。 | mixer 往返单元测试失败，检查矩阵元素量级后定位。 | 改用 Eigen `FullPivLU::isInvertible()`，重新验证力和力矩分配。 |
| 用一次性 `ros2 topic pub` 检查 RPM 时，建连时间超过安全超时，误以为电机命令无效。 | 对比时间戳和控制器超时日志。 | 改用 50 Hz 持续发布，确认实际转速稳定在约 10820.66 rpm。 |
| Matplotlib 样式名在 Ubuntu 自带的 3.5 版本中不可用。 | 实验脚本启动时报样式查找错误。 | 改用兼容的内置 rc 参数，并将绘图库延迟加载。 |
| 传感器噪声 reset 后随机序列不完全重复。 | 固定种子连续运行两次并逐样本比较。 | reset 随机引擎的同时重置正态分布对象，清除内部缓存。 |
| `pathlib.Path` 与 ROS 消息 `Path` 同名，导致记录器路径处理异常。 | 批处理保存结果时报类型错误。 | 将 ROS 消息显式别名为 `NavPath`，补充对应回归测试。 |
| Web 配置接口没有捕获 `yaml.YAMLError`，非法 YAML 会直接断开连接。 | 主动提交错误配置进行负向测试。 | 统一异常映射，稳定返回 HTTP 400，并验证不会覆盖原配置。 |
| 建议按墙钟补做动力学积分以追赶 200 Hz，导致圆、八字和避障场景退化。 | 完整 11 场景回归测试发现误差和规划表现变差。 | 撤销该修改，恢复一次回调推进一个固定 `dt`，没有放宽验收阈值。 |
| 高频重复发布 Marker 会加剧 TF 消息过滤和闪烁。 | 检查 Marker/TF 频率、发布者数量和 RViz 状态。 | 清理重复发布者，使用稳定 ID、合适 QoS 和 1 Hz 模型刷新。 |

## 六、本人对结果的验证方法

### 1. 数学和核心库验证

本人使用无 ROS 的 standalone CMake/CTest 检查电机一阶响应、悬停平衡、混控往返、四元数归一化、传感器确定性、轨迹连续性和控制闭环。理论悬停转速与仿真稳定转速相符，证明至少在单位和静态平衡层面不存在明显错误。

### 2. ROS2 接口验证

本人检查 Odometry、RPM 命令、TF、Marker、点云、路径和传感器 topic 的消息类型、频率、QoS 和发布者数量；同时通过 `launch_testing` 验证节点启动、1.5 m 起飞、数据有限性和正常退出。最终工作区 15 个 package 可以完整构建，35 项测试全部通过。

### 3. 场景和安全验证

本人运行正式批处理并保留 CSV、JSON、运行日志和 rosbag 证据。最终 11/11 场景通过；悬停稳态误差约 0.0203 m；三组避障场景的实际净距约为 0.4196 m、0.4136 m 和 0.4889 m，均高于 0.30 m 合同；多机最小间距约为 0.786 m。11 份正式日志中未出现控制超时或保护事件。

### 4. 可视化和文档验证

本人在 WSLg/RViz2 中检查飞机模型、TF、轨迹、障碍物、局部点云、体素和规划路径是否一致；对最终报告进行逐页渲染，检查图中文字、箭头、表格和图片是否重叠或裁切。报告中的数值均来自正式实验产物，不用手工编造数据。

## 七、总结

AI 在本项目中承担的是开发助手角色，帮助减少重复劳动、提供候选实现和加快问题排查。本人承担了核心公式与接口确认、架构边界、参数选择、错误定位、方案取舍以及最终验收。特别是对 AI 建议中出现的 mixer 判定、随机数重置、YAML 异常、时间调度和 RViz 发布策略等问题，均通过本人设计的测试或实际运行发现并修正。最终提交内容以可复现实验和测试结果为依据，而不是直接把 AI 输出当作正确答案。
