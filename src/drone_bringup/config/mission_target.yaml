waypoint_mission_node:
  ros__parameters:
    # x, y, z, yaw
    waypoints: [2.0, 1.0, 1.5, 0.0]
    arrival_tolerance: 0.12
    dwell_time: 0.8
    start_delay: 0.5

position_controller_node:
  ros__parameters:
    auto_takeoff: false

experiment_recorder:
  ros__parameters:
    scenario: target
    duration: 16.0
    output_dir: artifacts/experiments/target
